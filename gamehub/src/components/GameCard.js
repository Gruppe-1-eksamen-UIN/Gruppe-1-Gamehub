/*import React from "react";

function GameList({ game }) {
    return (
        
    <ul className="games">
       {game.map((games) => (
        <li key={games.title}>
        <h3>{games.title}</h3>
        </li>
       ))}
     </ul>
     );
        
  }

  export default GameList;
  */